PORT=3000
NODE_ENV=production

# Postgres connection string from the API container.
# "db" is the Docker Compose service name, not localhost.
DATABASE_URL=postgres://securenovation:securenovation_password@db:5432/securenovation

# Admin credentials for /api/login-user
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin_password

